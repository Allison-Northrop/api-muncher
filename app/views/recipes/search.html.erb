<h1 class="recipes">Recipes</h1>

<section class="recipe_links">

  <ul class="small-block-grid-3"><% @recipes.each do |recipe|  %>
      <h4><%= link_to "#{recipe.label}", show_recipe_path(recipe.uri) %><h4></li>
      <div><%= image_tag(recipe.image) %></div></br>

      <% end %>
    </ul>

</section>
